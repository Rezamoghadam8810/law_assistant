# ---------------------------
# ⚠️ فایل نمونه تنظیمات محیط
# برای هر پروژه جدید این رو کپی کن به اسم `.env`
# و مقادیر رو تغییر بده (به خصوص SECRET_KEY)
# ---------------------------

# نام اپلیکیشن
APP_NAME=Law Assistant

# محیط اجرا (dev / prod)
ENVIRONMENT=dev

# اتصال دیتابیس (Postgres)
DATABASE_URL=postgresql+psycopg2://postgres:yourpassword@localhost:5432/law_db

# ---------------------------
# 🔐 تنظیمات امنیتی JWT
# ---------------------------

# کلید مخفی برای امضای JWT
# ⚠️ حتماً در هر پروژه مقدار جدید و تصادفی تولید کن
# مثلا با دستور: openssl rand -hex 32
SECRET_KEY=change_this_secret_in_production

# الگوریتم امضا (معمولاً HS256)
ALGORITHM=HS256

# مدت اعتبار توکن (به دقیقه)
ACCESS_TOKEN_EXPIRE_MINUTES=30
